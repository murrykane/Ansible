- name: Install packages
  apt:
    name:
      - docker.io
      - openssl
      - ufw
      - docker-compose 
    state: present
    update_cache: true

- name: Enable docker
  systemd:
    name: docker
    enabled: true
    state: started